pipeline {
    
    agent any
    tools {
        nodejs 'node18'
    }

    stages {
        stage('Git-Pull') {
            steps {
                 git 'https://github.com/Vandal-William/GitHub-jenkins-project.git'
            }
        }
        stage('install') {

            steps {
                sh 'yarn'
                sh 'yarn build'
                dir('app') {
                    sh 'yarn' 
                }
            }
        }
        stage('Ansible') {

            steps {
                sh 'nunjucks-precompile ./app/hosts.yml.j2 -o ./app/hosts.js'
                sh 'node ./app/generate-hosts.js'
                sh 'ansible-playbook -i ./app/hosts.yml playbook.yml'    
            }
        }
    }

    post {
        success {
            sh 'echo sucess'
            // Ici, vous pouvez ajouter des étapes supplémentaires à effectuer après le succès de la construction
        }

        failure {
            // Ici, vous pouvez ajouter des étapes supplémentaires à effectuer en cas d'échec de la construction
            sh 'echo Erreur'
        }
    }
}