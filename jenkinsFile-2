pipeline {
    agent any
    stages {
        stage('Open directory in VS Code') {
            steps {
                script {
                    sh """
                        export DISPLAY=:2 &&
                        /snap/bin/code /var/lib/jenkins/workspace/GitHub-jenkins-project
                    """
                }
            }
        }
    }
}